import { config } from "dotenv";

 config()
  export const urlDB = process.env.POSTGRES_URL
  export const port = process.env.PORT;
  export const secret_pass = process.env.SECRET_PASS;
  export const path = process.env.rute;


